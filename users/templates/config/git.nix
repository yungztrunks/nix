{ ... }:

{
  # Configure and uncomment when user identity is set.
  # programs.git = {
  #   enable = true;
  #   settings = {
  #     user.name = userConfig.gitName or userConfig.username or "github-username";
  #     user.email = userConfig.gitEmail or "ID+github-username@users.noreply.github.com";
  #     init.defaultBranch = "main";
  #     pull.rebase = false;
  #   };
  # };
}
